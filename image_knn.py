'''
knn model for image description
'''

class KNN:
    '''
    knn model for generating description for images
    '''
    def __init__(self, params=None):
        '''
        initial a knn classifier
        :param params: when the params, do not need to train model.(may only useful for cnn)
        '''
        if params != None:
            # read params from file and initial model without training the model
            pass
        else:
            pass

    def fit(self, examples):
        '''
        fit in training examples to train the model
        :param examples: training examples
        :return:
        '''
        pass